<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit <%= post.title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <link rel="stylesheet" href="/stylesheets/showEdit.css" />
</head>

<body>

  <main class="edit-container">
    <div class="edit-form-container">
      <h1>Edit Post</h1>

      <form action="/posts/<%= post._id %>?_method=PUT" method="POST" enctype="multipart/form-data">
        <div class="form-row">
          <label for="title">Title</label>
          <input type="text" name="title" id="title" value="<%= post.title %>" required />
        </div>

        <div class="form-row">
          <label for="category">Category</label>
          <select name="category" id="category" required>
            <option value="">Select category</option>
            <option value="Shows" <%= post.category === 'Shows' ? 'selected' : '' %>>Shows</option>
            <option value="Books" <%= post.category === 'Books' ? 'selected' : '' %>>Books</option>
            <option value="Quotes" <%= post.category === 'Quotes' ? 'selected' : '' %>>Quotes</option>
            <option value="Music" <%= post.category === 'Music' ? 'selected' : '' %>>Music</option>
          </select>
        </div>

        <div class="form-row">
          <label for="description">Description</label>
          <textarea name="description" id="description"><%= post.description %></textarea>
        </div>

        <div class="form-row">
          <label for="link">Link (optional)</label>
          <input type="text" name="link" id="link" value="<%= post.link %>" />
        </div>

        <div class="form-row">
          <label for="image">Replace Image (optional)</label>
          <input type="file" name="image" id="image" />
        </div>

        <div class="btn-row">
          <button type="submit" class="round-btn primary-btn">📝 Update Post</button>
          <a href="/posts/<%= post._id %>" class="round-btn secondary-btn">↩ Back</a>
        </div>
      </form>
    </div>
  </main>
</body>
</html>
