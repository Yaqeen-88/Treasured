<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/showEdit.css" />
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title>Edit Post</title>
</head>
<body>
  <div class="edit-container">
    <section class="edit-form-container">
      <h1>Edit Post</h1>
        <form action="/posts/<%= post._id %>?_method=PUT" method="POST">

          <div class="form-row">
            <label for="title">Title</label>
            <input type="text" name="title" id="title" value="<%= post.title %>" required>
          </div>

          <div class="form-row">
            <label for="description">Description</label>
            <textarea name="description" id="description" rows="3"><%= post.description %></textarea>
          </div>

          <div class="form-row">
            <label for="category">Category</label>
            <select name="category" id="category" required>
              <option value="Songs" <%= post.category === 'songs'  ? 'selected' : '' %>>Songs</option>

              <option value="Pets" <%= post.category === 'pets'  ? 'selected' : '' %>>Pets</option>

              <option value="Moment" <%= post.category === 'moment'  ? 'selected' : '' %>>Moment</option>

              <option value="Food" <%= post.category === 'food'  ? 'selected' : '' %>>Food</option>

              <option value="Quote" <%= post.category === 'quote'  ? 'selected' : '' %>>Quote</option>

              <option value="Movies" <%= post.category === 'movies'  ? 'selected' : '' %>>Movies</option>

              <option value="Shows" <%= post.category === 'shows'  ? 'selected' : '' %>>Shows</option>

            </select>
          </div>

          <div class="btn-row">
            <button type="submit" class="btn-p">Update Post</button>
            <a href="/posts/<%= post._id %>" class="btn-g">Back </a>
          </div>
        </form>
    </section>
  </div>
</body>
</html>
